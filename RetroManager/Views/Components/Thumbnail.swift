import SwiftUI

struct Thumbnail: View {
    
    var thumbnailPath: String
    
    var body: some View {
        Text("Thumbnail")
    }
}

#Preview {
    Thumbnail(thumbnailPath: "ThumbnailPath")
}
